# currency-converter
An npm package to convert currency

npm
npm i t-currency-converter

import
import { currencyConverter } from "t-currency-converter";

convert
currencyConverter("USD", "INR", 2).then((data) => console.log(data));
